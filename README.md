# Fall25-ML-Project
We analyze high-dimensional data using PCA, CNN features, K-Means clustering, and t-SNE visualization. These methods reveal meaningful structure in the embedding space, and we use this representation to perform similarity search that returns the top-5 matches for each query.
